# Shared non-secret defaults for local dev.
# Secrets and overrides live in .env.local (gitignored).
# This file is committed so the Prisma CLI (which only reads .env)
# can find DATABASE_URL for migrate/generate commands.

DATABASE_URL="postgresql://expense:expense_dev@localhost:5433/expense_tracker?schema=public"
